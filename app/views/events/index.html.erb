
<h1><%= @location.name %></h1>
<%= image_tag(@location.image) %>

<%= @events.each do |event| %>
  <ul>
    <li> <%= event.name %></li>
    <li> <%= event.date %> </li>
    <li><%= link_to "Edit", edit_location_event_path(@location,event) %> |
        <%= link_to "View", location_event_path(@location,event) %> </li>
  </ul>

<% end %>
